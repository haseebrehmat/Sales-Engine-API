version: '3.8'

services:
  web:
    build: .
#    command: python manage.py runserver --settings=settings.production 0.0.0.0:8000
    command: gunicorn --bind 0.0.0.0:8000 settings.wsgi
    volumes:
      - ./:/usr/src/app/
    ports:
      - 8000:8000
